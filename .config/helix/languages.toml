[[language]]
name = "python"
roots = ["pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "poetry.lock"]
language-servers = ["pyright"]
auto-format = true
formatter = { command = "ruff", args = ["format", "-"]}
